#ifndef pattern_calls_h
#define pattern_calls_h
#include<iostream> 
using namespace std;
class calls{ 
tuple <int , string, string , string> 

void find_call(int id, string name, string patternFile, string Flag){
    return make_tuple(id,name, patternFile, Flag); 
	}; 
}
